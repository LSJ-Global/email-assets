# 0529_edm

- Server HTML/EML files use HTTPS absolute image URLs with this placeholder base: https://your-https-server.example/0529_edm/images
- Upload the `images` folder to an HTTPS server, then replace that base URL in all `.html` and `.eml` files with the final hosted image path.
- `_local.html` files are for local preview and use relative image paths.
- CTA is the only linked image segment. No image map, script, or full-image link is used.
- All image slices are 700px wide or narrower and 1000px high or shorter.
